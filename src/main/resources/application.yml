server:
  port: 8080

spring:
  application:
    name: api-weather

  profiles:
    active: dev

  flyway:
    enabled: true
    locations: classpath:db/migration

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html